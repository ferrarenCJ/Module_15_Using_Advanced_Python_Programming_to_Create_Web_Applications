## Virtual Environment (venv)

The `venv` folder contains the local Python virtual environment used during Module 15 development.

```text
venv/
```

### Purpose

The virtual environment isolates project dependencies from the global Python installation.

Benefits include:

- Dependency 